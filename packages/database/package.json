{
  "name": "database",
  "version": "1.0.0",
  "description": "Camada de acesso ao banco de dados (Supabase)",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.117.2",
    "server-only": "^0.0.1"
  }
}
