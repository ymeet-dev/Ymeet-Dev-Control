# Segurança

Esta pasta documentará as políticas de segurança do sistema: autenticação, autorização por papel, políticas de RLS por tabela e o mecanismo de auditoria (`logs`, `task_state_transitions`).

Conteúdo detalhado (políticas de RLS por tabela) a ser adicionado quando as migrations do Supabase forem criadas.
