# Ymeet Dev Control — variáveis de ambiente
# Copie este arquivo para .env.local (apps/web) e preencha com valores reais.
# Nenhum valor real deve ser commitado.

# Supabase (configuração definitiva prevista para Fase 2+)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App
NODE_ENV=development
